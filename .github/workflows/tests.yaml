name: Tests

on: 
  push:
  pull_request:
  workflow_dispatch:

jobs:


  ubuntu:
    runs-on: ${{ matrix.os }}
    environment: UNIT Tests  
    strategy:
      matrix:
        os: [ubuntu-latest]
    env:
      OS: ${{ matrix.os }}
      PYTHON: '3.8'
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: Install dependencies
      run: |
        sudo apt-get install redis-server git-lfs
        git lfs pull
        sudo python -m pip install --upgrade pip
        sudo pip install pytest
        sudo pip3 install -r requirements.txt
    - name: Test with pytest
      run: |
        cd tests
        sudo pytest -v

